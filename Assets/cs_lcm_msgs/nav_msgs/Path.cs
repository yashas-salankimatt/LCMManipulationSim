/* LCM type definition class file
 * This file was automatically generated by lcm-gen 1.5.1
 * DO NOT MODIFY BY HAND!!!!
 */

using System;
using System.Collections.Generic;
using System.IO;
using LCM.LCM;
 
namespace nav_msgs
{
    public sealed class Path : LCM.LCM.LCMEncodable
    {
        public int poses_length;
        public std_msgs.Header header;
        public geometry_msgs.PoseStamped[] poses;
 
        public Path()
        {
        }
 
        public static readonly ulong LCM_FINGERPRINT;
        public static readonly ulong LCM_FINGERPRINT_BASE = 0xc779b6acc503055aL;
 
        static Path()
        {
            LCM_FINGERPRINT = _hashRecursive(new List<String>());
        }
 
        public static ulong _hashRecursive(List<String> classes)
        {
            if (classes.Contains("nav_msgs.Path"))
                return 0L;
 
            classes.Add("nav_msgs.Path");
            ulong hash = LCM_FINGERPRINT_BASE
                 + std_msgs.Header._hashRecursive(classes)
                 + geometry_msgs.PoseStamped._hashRecursive(classes)
                ;
            classes.RemoveAt(classes.Count - 1);
            return (hash<<1) + ((hash>>63)&1);
        }
 
        public void Encode(LCMDataOutputStream outs)
        {
            outs.Write((long) LCM_FINGERPRINT);
            _encodeRecursive(outs);
        }
 
        public void _encodeRecursive(LCMDataOutputStream outs)
        {
            outs.Write(this.poses_length); 
 
            this.header._encodeRecursive(outs); 
 
            for (int a = 0; a < this.poses_length; a++) {
                this.poses[a]._encodeRecursive(outs); 
            }
 
        }
 
        public Path(byte[] data) : this(new LCMDataInputStream(data))
        {
        }
 
        public Path(LCMDataInputStream ins)
        {
            if ((ulong) ins.ReadInt64() != LCM_FINGERPRINT)
                throw new System.IO.IOException("LCM Decode error: bad fingerprint");
 
            _decodeRecursive(ins);
        }
 
        public static nav_msgs.Path _decodeRecursiveFactory(LCMDataInputStream ins)
        {
            nav_msgs.Path o = new nav_msgs.Path();
            o._decodeRecursive(ins);
            return o;
        }
 
        public void _decodeRecursive(LCMDataInputStream ins)
        {
            this.poses_length = ins.ReadInt32();
 
            this.header = std_msgs.Header._decodeRecursiveFactory(ins);
 
            this.poses = new geometry_msgs.PoseStamped[(int) poses_length];
            for (int a = 0; a < this.poses_length; a++) {
                this.poses[a] = geometry_msgs.PoseStamped._decodeRecursiveFactory(ins);
            }
 
        }
 
        public nav_msgs.Path Copy()
        {
            nav_msgs.Path outobj = new nav_msgs.Path();
            outobj.poses_length = this.poses_length;
 
            outobj.header = this.header.Copy();
 
            outobj.poses = new geometry_msgs.PoseStamped[(int) poses_length];
            for (int a = 0; a < this.poses_length; a++) {
                outobj.poses[a] = this.poses[a].Copy();
            }
 
            return outobj;
        }
    }
}

